name: anka_super_loading_package_example
description: Demo app for the anka_super_loading_package library.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  anka_super_loading_package:
    path: ../

  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/screenshots/
    - assets/branding/anka_club_logo.png
